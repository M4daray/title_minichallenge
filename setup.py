from setuptools import setup, find_packages

setup(name='title',
      version='0.0.1',
      description='',
      url='',
      author='Brivael SANCHEZ',
      author_email='',
      packages=find_packages(where='src'),
      package_dir={'':'src'})